<view class="empty-state">
  <text class="empty-icon">{{icon}}</text>
  <text class="empty-title">{{title}}</text>
  <text class="empty-desc">{{desc}}</text>
  <view wx:if="{{btnText}}" class="empty-btn" bind:tap="onBtnTap">
    <text>{{btnText}}</text>
  </view>
</view>
